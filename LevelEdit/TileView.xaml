<UserControl x:Class="LevelEdit.TileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LevelEdit"
             mc:Ignorable="d" 
             d:DesignHeight="32" d:DesignWidth="32" SizeChanged="UserControl_SizeChanged">
    <Grid>
        <Border x:Name="border" BorderBrush="Black" BorderThickness="1">
            <Border.Background>
                <SolidColorBrush Color="#FF848484" Opacity="0.25"/>
            </Border.Background>
            <Image  x:Name="image" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" RenderOptions.BitmapScalingMode="NearestNeighbor" MouseEnter="image_MouseEnter" MouseLeave="image_MouseLeave" MouseUp="image_MouseUp" MouseDown="image_MouseDown"/>
        </Border>
    </Grid>
</UserControl>
